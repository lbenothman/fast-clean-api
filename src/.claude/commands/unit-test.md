Create an unit test for the use case $1
- Always use fixture instead of creating new instances of objects or entities for the tests
- Always use existing fixtures for the tests, you can add new if needed
- Never run the tests